# KUBELET demo:
hostname
# now in the node
PS1="k8s-node> "
lsns
ps -ef | grep kubelet
cat /etc/kubernetes/kubelet/kubelet-config.json
cat /etc/resolv.conf

lsns 
sudo bash
lsns 
pstree
ps f -e

exit   # out of sudo
exit   # out of aws ssm start-session (node)
